---
version: '2'

services:

  registry:
    image: couchdb
    ports:
      - 5984:5984
    #volumes:
    #  - ./registry/data:/usr/local/var/lib/couchdb
  
  datacenters:
    image: couchdb
    ports:
      - 5985:5984

  hosts:
    image: couchdb
    ports:
      - 5986:5984

  services:
    image: couchdb
    ports:
      - 5987:5984

  proxy:
    image: nginx:alpine
    ports:
      - 8080:80
    volumes:
      - ./proxy/conf.d/:/etc/nginx/conf.d/:ro
